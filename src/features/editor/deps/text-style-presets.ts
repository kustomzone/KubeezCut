/**
 * Text style presets (timeline-owned definitions); re-export for editor UI.
 */
export {
  PLAIN_PRESET,
  TEXT_STYLE_PRESETS,
  getTextStylePresetOverridesById,
  type TextStylePreset,
} from './timeline-contract';
